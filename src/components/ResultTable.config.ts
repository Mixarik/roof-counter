import { ResultTableEnum } from './ResultTable.enum';

export const thConfig: { [key: string]: string } = {
	name: ResultTableEnum.Name,
	unit: ResultTableEnum.Unit,
	amount: ResultTableEnum.Amount,
	price: ResultTableEnum.Price,
};

export const trConfig = [
	{

	}
]
